'use strict';

function simpleMathOps(){
    // Addition  +
    // Subtraction -
    // Multiplaction *
    // Division /
    // Exponentiation **
    // Modulus %
    // Increment ++
    // Decrement --
}


// Plus sign is overloaded. 
  // For string = concatentation; if one of string againg will concatentation
  // For numbers = addition



function assignmentOps() {
    // equal(=) > price = 10;
    // addition(+=) >  price += 5;
    // subtraction (-=) > price -= 2;
    // multiplaction (*=) > price  *= 3;
    // division (/=) > price /= 10;
    // exponentiation (**=) > price **= 2;
    // modulus (%=) > price %= 3;
    
}

function comparasionOps() {
    // equal in value (==)
    // equal in value and type (===)
    
}

function logicalOps() {
    // any variable with a value or a Boolean true is true
    // any variable = false, null, undefined, NaN, "" is false
    
}

