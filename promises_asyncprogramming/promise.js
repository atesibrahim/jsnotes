// Promise : Object that represents the eventual completion (or failure) of an asynchronous operation, and its resulting value
  // states
    // pending
    // fulfilled
    // rejected : when async call is failed
    // settled
    // resolved
// Promises are not lazy

// Promise object

let promise = new Promise();